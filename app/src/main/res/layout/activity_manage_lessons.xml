<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_manage_lessons"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Tiêu đề -->
    <TextView
        android:id="@+id/tvManageLessonsTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Quản lý bài giảng"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@android:color/black" />

    <!-- Danh sách chương -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerLessons"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tvManageLessonsTitle"
        android:layout_marginTop="12dp"
        android:layout_above="@+id/btnAddLesson"
        android:clipToPadding="false"
        android:paddingBottom="80dp" />

    <!-- Nút thêm bài giảng -->
    <Button
        android:id="@+id/btnAddLesson"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="＋"
        android:textSize="28sp"
        android:textColor="@android:color/white"
        android:backgroundTint="@color/teal_700"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="16dp"
        android:background="@drawable/fab_background"
        android:elevation="6dp" />

</RelativeLayout>
